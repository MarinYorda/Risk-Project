Map Class:
- Add player objects to territories
- Vectors (adjacency list, continents)
- Constructors
- Map constructor
- A method that adds either continets or territories to a vector
- validate method in map class, that checks:
    1.the map is a connected graph
    2.continents are connected subgraphs
    3.each country belongs to one and only one continent
- The map loader must be able to read any map file available on the above-mentioned web page.
- A driver (free function) named testLoadMaps() that reads many different map files, creates a Map object for valid files and rejects the invalid ones.

